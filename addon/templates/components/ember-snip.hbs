{{#if hasBlock}}
  {{yield
    (hash
      snipee = (component snipee
        rectangle         = _rectangle
        _offsetTop        = offsetTop
        _offsetRight      = offsetRight
        _offsetBottom     = offsetBottom
        _offsetLeft       = offsetLeft
        _restrictToOffset = cancelOffset
        _dimensions       = _dimensions
      )
    )
    }}
{{else}}
  {{component snipee
    rectangle         = _rectangle
    _offsetTop        = offsetTop
    _offsetRight      = offsetRight
    _offsetBottom     = offsetBottom
    _offsetLeft       = offsetLeft
    _restrictToOffset = cancelOffset
    _dimensions       = _dimensions
    }}
{{/if}}
